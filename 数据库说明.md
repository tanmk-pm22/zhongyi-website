# 数据库说明文档

## 📊 数据库类型

**SQLite** - 轻量级本地数据库

### 为什么选择 SQLite？
- ✅ 不需要安装数据库服务器
- ✅ 数据保存在本地文件中
- ✅ 简单易用，适合小白
- ✅ 完全免费
- ✅ 数据永久保存（不会因重启丢失）

---

## 📁 数据库文件位置

数据库文件保存在：
```
项目根目录/data/patients.db
```

**重要**：这个文件夹已经在 `.gitignore` 中，不会上传到 GitHub，保护患者隐私。

---

## 🗄️ 数据表结构

### 1. patients 表（患者信息）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 患者编号（主键，如：FT1A2B3C） |
| fullName | TEXT | 姓名 |
| gender | TEXT | 性别 (male/female) |
| dateOfBirth | TEXT | 出生日期 |
| idType | TEXT | 证件类型 (ic/passport) |
| idNumber | TEXT | 证件号码 |
| phone | TEXT | 电话号码 |
| email | TEXT | 电子邮件 |
| address | TEXT | 街道地址 |
| city | TEXT | 城市 |
| state | TEXT | 州属 |
| postcode | TEXT | 邮编 |
| country | TEXT | 国家 |
| emergencyContact | TEXT | 紧急联系人姓名 |
| emergencyPhone | TEXT | 紧急联系人电话 |
| emergencyRelation | TEXT | 与患者关系 |
| allergies | TEXT | 药物过敏史 |
| medicalHistory | TEXT | 过往病史 |
| currentMedications | TEXT | 目前服用药物 |
| createdAt | TEXT | 注册时间 |
| updatedAt | TEXT | 更新时间 |

### 2. medical_records 表（就诊记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 记录编号（自动递增） |
| patientId | TEXT | 患者编号（外键） |
| date | TEXT | 就诊日期 |
| doctor | TEXT | 医师姓名 |
| diagnosis | TEXT | 诊断 |
| treatment | TEXT | 治疗方案 |
| prescription | TEXT | 处方 |
| notes | TEXT | 备注 |
| createdAt | TEXT | 创建时间 |

---

## 🔍 查询索引

已创建以下索引以提高查询速度：
- `idx_patients_phone` - 按电话号码查询
- `idx_patients_idNumber` - 按身份证号码查询
- `idx_medical_records_patientId` - 按患者ID查询就诊记录

---

## 💾 数据备份

### 重要提醒
**请定期备份数据库文件！**

### 备份方法

#### 方法1：手动备份
1. 找到 `data/patients.db` 文件
2. 复制到安全位置（如U盘、云盘）
3. 建议每天备份一次

#### 方法2：自动备份（推荐）
可以使用以下脚本自动备份：

**Windows (backup.bat):**
```batch
@echo off
set backupDir=备份\%date:~0,10%
mkdir "%backupDir%" 2>nul
copy data\patients.db "%backupDir%\patients_%time:~0,2%%time:~3,2%.db"
echo 备份完成！
```

**Mac/Linux (backup.sh):**
```bash
#!/bin/bash
backupDir="备份/$(date +%Y-%m-%d)"
mkdir -p "$backupDir"
cp data/patients.db "$backupDir/patients_$(date +%H%M).db"
echo "备份完成！"
```

---

## 🔄 数据恢复

如果需要恢复数据：

1. 停止网站服务器
2. 删除当前的 `data/patients.db` 文件
3. 将备份的数据库文件复制到 `data/` 文件夹
4. 重命名为 `patients.db`
5. 重启服务器

---

## 📈 数据库维护

### 查看数据库大小
```bash
ls -lh data/patients.db
```

### 优化数据库（定期执行）
数据库会自动维护，无需手动操作。

如需优化，可以使用 SQLite 工具：
```bash
sqlite3 data/patients.db "VACUUM;"
```

---

## 🔐 数据安全

### 已实施的安全措施
1. ✅ 数据库文件不会上传到 GitHub
2. ✅ 使用参数化查询防止 SQL 注入
3. ✅ 患者隐私信息加密存储

### 建议的额外措施
- [ ] 定期更改服务器密码
- [ ] 使用 HTTPS 加密传输
- [ ] 限制数据库文件访问权限
- [ ] 定期备份到异地

---

## 🆙 升级到云数据库

如果未来需要升级到云数据库（多人访问、更大容量），可选择：

### 选项1：Vercel Postgres（推荐）
- 与 Vercel 完美集成
- 免费额度充足
- 自动备份

### 选项2：MongoDB Atlas
- NoSQL 数据库
- 免费 512MB
- 全球分布式

### 选项3：PlanetScale
- MySQL 兼容
- 免费 5GB
- 自动扩展

---

## 📞 常见问题

### Q1：数据库文件在哪里？
**A**：项目根目录的 `data/patients.db`

### Q2：重启服务器数据会丢失吗？
**A**：不会！SQLite 数据保存在文件中，永久保存。

### Q3：如何查看数据库内容？
**A**：可以使用以下工具：
- DB Browser for SQLite（免费，图形界面）
- SQLite 命令行工具
- VS Code 的 SQLite 插件

### Q4：数据库文件太大怎么办？
**A**：
- 定期删除旧的就诊记录
- 或升级到云数据库

### Q5：如何导出数据？
**A**：使用 SQLite 工具导出为 CSV 或 JSON 格式

---

## 🛠️ 开发工具推荐

### DB Browser for SQLite
**下载**: https://sqlitebrowser.org/

**功能**：
- 可视化查看数据
- 执行 SQL 查询
- 导入导出数据
- 修改表结构

### VS Code 插件
**插件名**: SQLite

**安装**：
1. 打开 VS Code
2. 搜索 "SQLite"
3. 安装并启用

---

**如有问题，请参考主文档或联系技术支持！** 📖
